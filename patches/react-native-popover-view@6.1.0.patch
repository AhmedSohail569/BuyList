
diff --git a/node_modules/react-native-popover-view/dist/AdaptivePopover.js b/node_modules/react-native-popover-view/dist/AdaptivePopover.js
index 79d7acc..e6c8f0f 100644
--- a/node_modules/react-native-popover-view/dist/AdaptivePopover.js
+++ b/node_modules/react-native-popover-view/dist/AdaptivePopover.js
@@ -256,13 +256,15 @@ var AdaptivePopover = /** @class */ (function (_super) {
                         _d.label = 1;
                     case 1:
                         if (!!(fromRef === null || fromRef === void 0 ? void 0 : fromRef.current)) return [3 /*break*/, 3];
+
+                        if (!this._isMounted) return [2 /*return*/];
                         return [4 /*yield*/, new Promise(function (resolve) {
                                 setTimeout(resolve, 100);
                             })];
                     case 2:
                         _d.sent();
                         // Timeout after 2 seconds
-                        if (count++ > 20)
+                        if (count++ > 20 || !this._isMounted)
                             return [2 /*return*/];
                         return [3 /*break*/, 1];
                     case 3:
@@ -287,13 +289,14 @@ var AdaptivePopover = /** @class */ (function (_super) {
                         if (count === 0 && AdaptivePopover.hasRetrievedSatisfyingRect(rect, initialRect)) {
                             return [3 /*break*/, 8];
                         }
+                        if (!this._isMounted) return [2 /*return*/];
                         return [4 /*yield*/, new Promise(function (resolve) {
                                 setTimeout(resolve, 100);
                             })];
                     case 6:
                         _d.sent();
                         // Timeout after 2 seconds
-                        if (count++ > 20)
+                        if (count++ > 20 || !this._isMounted)
                             return [2 /*return*/];
                         _d.label = 7;
                     case 7:
diff --git a/node_modules/react-native-popover-view/dist/ b/node_modules/react-native-popover-view/dist/
index 8a63627cd8ef4521edc8a79806d2ff8862f56e52..3aa8b72698841c4941e38d4f17a56c5cd2714080 100644
--- a/node_modules/react-native-popover-view/dist/BasePopover.js
+++ b/node_modules/react-native-popover-view/dist/BasePopover.js
@@ -176,7 +176,7 @@ var BasePopover = /** @class */ (function (_super) {
                     else if (!activeGeom) {
                         _this.debug('handleChange - animating in');
                         if (onOpenStart)
-                            setTimeout(onOpenStart);
+                            onOpenStart();
                         _this.animateIn();
                     }
                     else if (activeGeom && !Geometry.equals(activeGeom, geom_1)) {
@@ -244,7 +244,7 @@ var BasePopover = /** @class */ (function (_super) {
     BasePopover.prototype.animateOut = function () {
         var _this = this;
         if (this.props.onCloseStart)
-            setTimeout(this.props.onCloseStart);
+            this.props.onCloseStart();
         if (this._isMounted)
             this.setState({ showing: false });
         this.debug('animateOut - isMounted', this._isMounted);
@@ -253,7 +253,7 @@ var BasePopover = /** @class */ (function (_super) {
             fade: 0,
             scale: 0,
             translatePoint: this.getTranslateOrigin(),
-            callback: function () { return setTimeout(_this.props.onCloseComplete); },
+            callback: function () { return _this.props.onCloseComplete(); },
             easing: Easing.inOut(Easing.quad),
             geom: this.getGeom()
         });
@@ -282,18 +282,16 @@ var BasePopover = /** @class */ (function (_super) {
                     if (_this._isMounted) {
                         _this.setState({ showing: true });
                         if (_this.props.debug || DEBUG) {
-                            setTimeout(function () {
-                                return _this.popoverRef.current &&
-                                    getRectForRef(_this.popoverRef).then(function (rect) { return _this.debug('animateIn - onOpenComplete - Calculated Popover Rect', rect); });
-                            });
-                            setTimeout(function () {
-                                return _this.arrowRef.current &&
-                                    getRectForRef(_this.arrowRef).then(function (rect) { return _this.debug('animateIn - onOpenComplete - Calculated Arrow Rect', rect); });
-                            });
+                            if (_this.popoverRef.current) {
+                              getRectForRef(_this.popoverRef).then(function (rect) { return _this.debug('animateIn - onOpenComplete - Calculated Popover Rect', rect); });
+                            }
+                            if (_this.arrowRef.current) {
+                              getRectForRef(_this.arrowRef).then(function (rect) { return _this.debug('animateIn - onOpenComplete - Calculated Arrow Rect', rect); });
+                            }
                         }
                     }
                     if (_this.props.onOpenComplete)
-                        setTimeout(_this.props.onOpenComplete);
+                        _this.props.onOpenComplete();
                     if (_this.animateOutAfterShow || !_this._isMounted) {
                         _this.animateOut();
                         _this.animateOutAfterShow = false;
@@ -307,7 +305,8 @@ var BasePopover = /** @class */ (function (_super) {
         var fade = args.fade, translatePoint = args.translatePoint, scale = args.scale, callback = args.callback, easing = args.easing, values = args.values;
         var commonConfig = __assign({ duration: 300, easing: easing, useNativeDriver: !isWeb }, this.props.animationConfig);
         if (this.animating) {
-            setTimeout(function () { return _this.animateTo(args); }, 100);
+            if(this._isMounted)
+              setTimeout(function () { return _this.animateTo(args); }, 100);
             return;
         }
         // eslint-disable-next-line
@@ -412,8 +411,8 @@ var BasePopover = /** @class */ (function (_super) {
                     React.createElement(Animated.View, { style: transformStyle },
                         React.createElement(View, { ref: this.popoverRef, style: contentWrapperStyle, onLayout: function (evt) {
                                 var layout = __assign({}, evt.nativeEvent.layout);
-                                setTimeout(function () { return _this._isMounted &&
-                                    _this.measureContent(new Size(layout.width, layout.height)); }, 10);
+                                if(_this._isMounted)
+                                    _this.measureContent(new Size(layout.width, layout.height));
                             } }, this.props.children),
                         geom.placement !== Placement.FLOATING &&
                             React.createElement(Arrow, { ref: this.arrowRef, placement: geom.placement, color: backgroundColor, arrowSize: arrowSize, positionStyle: arrowPositionStyle, elevation: elevation }))))));
diff --git a/node_modules/react-native-popover-view/dist/RNModalPopover.js b/node_modules/react-native-popover-view/dist/RNModalPopover.js
index e9e611b56e13be63b6c111e293022f7eaf080293..851db508b069bef0278d2c8de2908dced568e33a 100644
--- a/node_modules/react-native-popover-view/dist/RNModalPopover.js
+++ b/node_modules/react-native-popover-view/dist/RNModalPopover.js
@@ -59,7 +59,7 @@ var RNModalPopover = /** @class */ (function (_super) {
              * Need to wait 50ms to make sure <Modal> is completely gone, in case
              * we want to show another popover immediately after
              */
-            setTimeout(this.props.onCloseComplete, 50);
+            this.props.onCloseComplete();
         }
     };
     RNModalPopover.prototype.render = function () {
@@ -68,7 +68,7 @@ var RNModalPopover = /** @class */ (function (_super) {
         var visible = this.state.visible;
         return (React.createElement(Modal, { transparent: true, supportedOrientations: ['portrait', 'portrait-upside-down', 'landscape'], hardwareAccelerated: true, visible: visible, statusBarTranslucent: statusBarTranslucent !== null && statusBarTranslucent !== void 0 ? statusBarTranslucent : DEFAULT_STATUS_BAR_TRANSLUCENT, onShow: function () {
                 RNModalPopover.isShowingInModal = true;
-            }, 
+            },
             // Handles android back button
             onRequestClose: onRequestClose },
             React.createElement(AdaptivePopover, __assign({}, this.props, { onCloseStart: function () {
diff --git a/node_modules/react-native-popover-view/dist/ b/node_modules/react-native-popover-view/dist/_AdaptivePopover.js
index a748dba893f8c93e18c9541e8683a12a84ca5377..ccfc1f33eaad9d171bc51b5697854f7e020a6506 100644
--- a/node_modules/react-native-popover-view/dist/_AdaptivePopover.js
+++ b/node_modules/react-native-popover-view/dist/_AdaptivePopover.js
@@ -256,13 +256,14 @@ var AdaptivePopover = /** @class */ (function (_super) {
                         _c.label = 1;
                     case 1:
                         if (!!(fromRef === null || fromRef === void 0 ? void 0 : fromRef.current)) return [3 /*break*/, 3];
+                        if (!this._isMounted) return [2 /*return*/];
                         return [4 /*yield*/, new Promise(function (resolve) {
                                 setTimeout(resolve, 100);
                             })];
                     case 2:
                         _c.sent();
                         // Timeout after 2 seconds
-                        if (count++ > 20)
+                        if (count++ > 20 || !this._isMounted)
                             return [2 /*return*/];
                         return [3 /*break*/, 1];
                     case 3:
@@ -282,13 +283,14 @@ var AdaptivePopover = /** @class */ (function (_super) {
                         if (count === 0 && AdaptivePopover.hasRetrievedSatisfyingRect(rect, initialRect)) {
                             return [3 /*break*/, 8];
                         }
+                        if (!this._isMounted) return [2 /*return*/];
                         return [4 /*yield*/, new Promise(function (resolve) {
                                 setTimeout(resolve, 100);
                             })];
                     case 6:
                         _c.sent();
                         // Timeout after 2 seconds
-                        if (count++ > 20)
+                        if (count++ > 20 || !this._isMounted)
                             return [2 /*return*/];
                         _c.label = 7;
                     case 7: